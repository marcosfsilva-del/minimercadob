# Atualizacao de subtotal do carrinho

Atualiza os subtotais dos itens e o total geral localmente quando a quantidade
do produto muda. A feature usa os slots `CART_ITEM` e `CART_SUMMARY`, sem
alterar o core ou buscar dados externos.
